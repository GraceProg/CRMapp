@if (User.Identity.IsAuthenticated)
{
@*    <form method="post" class="form-inline" action="/Identity/Account/Logout">
        Welcome @User.Identity.Name
        <button type="submit" class="m-lg-2 btn btn-link">Logout</button>
    </form>*@
    <a class="btn btn-link" href="/Identity/Account/Logout">Logout</a>
}
else
{
    <a class="btn btn-link" asp-page="/Identity/Account/Login">Login</a>
}